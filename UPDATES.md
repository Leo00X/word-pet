# 📅 WordParasite 功能更新日志

本文档记录 WordParasite 项目的主要功能更新和 Bug 修复。

---

## 🎉 2025年12月5日 - 聊天系统 Bug 修复 + 体验优化

### ✅ 已修复问题

#### 1. 聊天输入框无法输入文字 ⚠️ → ✅
**症状**: 用户在聊天输入框中输入文字时，界面不显示任何内容，点击发送按钮没有反应。

**根本原因**: Vue 3 响应式系统中，将 composable 返回的 ref 对象赋值给 `this.chat` 时，ref 被自动解包（unwrap）成普通值，导致响应性丢失。

**解决方案**:
1. 重构 `index.vue` 的 composable 初始化方式，将 `useChat()` 返回的 ref 单独存储
2. 更新 `ChatPanel.vue` props 定义，添加 ref 兼容逻辑
3. 添加计算属性安全地访问 ref 的值

**影响文件**:
- `pages/index/index.vue`
- `pages/index/components/ChatPanel.vue`

---

#### 2. 历史消息显示混乱 ⚠️ → ✅
**症状**: 
- 历史消息重复显示
- 消息顺序错乱
- 时间戳不友好（只显示时:分）

**根本原因**:
1. `useChat.js` 使用 `unshift()` 将新消息插入数组头部
2. 加载历史时已是倒序，再次 `reverse()` 导致顺序混乱
3. 时间显示固定格式，不够人性化

**解决方案**:
1. **修复消息存储顺序** - 改用 `push()` 保持时间正序
   ```javascript
   messages.value.push(message);
   if (messages.value.length > 100) {
     messages.value = messages.value.slice(-100);
   }
   ```

2. **移除反转逻辑** - `ChatPanel.vue` 直接显示消息
   ```javascript
   displayMessages() {
     return this.messagesValue;
   }
   ```

3. **优化时间显示** - `ChatBubble.vue` 实现相对时间
   ```javascript
   formatTime(timestamp) {
     const now = Date.now();
     const diff = now - timestamp;
     
     if (diff < 60000) return '刚刚';
     if (diff < 3600000) return `${Math.floor(diff/60000)}分钟前`;
     // ... 今天/昨天/日期
   }
   ```

4. **优化 UI 间距** - 更紧凑的布局
   - 消息间距: 15px → 10px
   - 头像尺寸: 40px → 35px

**影响文件** (100% 重写):
- `pages/index/composables/useChat.js`
- `pages/index/components/ChatPanel.vue`
- `pages/index/components/ChatBubble.vue`

**测试结果**:
- ✅ 消息按时间正序显示（旧消息在上，新消息在下）
- ✅ 历史记录无重复
- ✅ 时间显示友好（刚刚/5分钟前/昨天/12-04）
- ✅ 布局更紧凑，可显示更多消息

---

### 🎨 新增功能

#### 1. 相对时间显示
消息时间戳现在会智能显示：
- **1分钟内**: "刚刚"
- **1小时内**: "5分钟前"
- **今天**: "14:30"
- **昨天**: "昨天"
- **更早**: "12-04"

#### 2. 聊天历史分页加载
为了保持界面简洁，聊天面板默认只显示最近 3 条消息。

**使用方法**:
- 点击消息列表上方的 **"加载更早的消息 (X条)"** 按钮
- 一次加载 10 条历史消息
- 发送新消息后会重置为显示最近 3 条

**实现细节**:
```javascript
// ChatPanel.vue
displayMessages() {
  const total = this.messagesValue.length;
  if (total <= this.showCount) {
    return this.messagesValue;
  }
  return this.messagesValue.slice(total - this.showCount);
}
```

#### 3. 多轮对话上下文记忆
AI 现在能记住之前的对话内容！

**工作原理**:
- 发送消息时，自动提取最近的聊天历史
- 将历史对话传递给 AI 模型
- AI 可以基于之前的对话内容做出更连贯的回复

**支持的模型**:
- ✅ DeepSeek V3.2
- ✅ Gemini 2.5 Flash/Pro
- ✅ 所有自定义模型（兼容 OpenAI API）

**实现代码**:
```javascript
// index.vue - handleSendMessage
const history = this.chatMessages.value
  .filter(m => m.role !== 'system')
  .map(m => ({
    role: m.role === 'user' ? 'user' : 'assistant',
    content: m.content
  }));

const reply = await this.ai.chatWithPet(userMsg, ctx, history);
```

---

## 🤖 2025年12月5日 01:45 - AI 多模型系统

### 核心成果
- ✨ 创建了 **7 个新文件**（加密工具、适配器、服务层、配置管理、UI页面）
- 🔧 修改了 **3 个现有文件**（ConfigPanel、useAI、pages.json）
- 🎨 UI 采用赛博朋克风格，与主应用统一
- 🔒 API Key 使用 XOR 加密存储

### 支持的 AI 模型

#### 1. DeepSeek V3.2
- **特点**: 高性价比国产模型
- **速度**: 快
- **价格**: 极低
- **适合**: 日常对话、快速响应

#### 2. Gemini 2.5 Flash
- **特点**: Google 快速响应模型
- **速度**: 极快
- **上下文**: 1M tokens
- **适合**: 简短对话、快速互动

#### 3. Gemini 2.5 Pro
- **特点**: Google 高级推理模型
- **能力**: 复杂推理、长上下文
- **上下文**: 2M tokens
- **适合**: 复杂任务、深度对话

#### 4. 自定义模型
- **标准**: 兼容 OpenAI API 格式
- **灵活性**: 可接入任意第三方 API
- **示例**: ChatGPT、Claude、本地模型等

### 关键发现

Gemini 已支持 OpenAI 兼容端点，代码极大简化：
```javascript
// 使用标准 OpenAI 格式，无需复杂转换
const url = 'https://generativelanguage.googleapis.com/v1beta/openai/chat/completions';

// 请求格式与 OpenAI 完全一致
const body = {
  model: 'gemini-2.5-flash',
  messages: [...],
  max_tokens: 500
};
```

### 使用方法

1. **进入配置页面**
   - 打开 App → 系统设置 → 🤖 AI 模型选择

2. **配置 API Key**
   - 点击模型卡片上的"配置密钥"按钮
   - 输入 API Key
   - 点击"测试连接"验证

3. **获取 API Key**
   - **Gemini**: [https://aistudio.google.com/apikey](https://aistudio.google.com/apikey)
   - **DeepSeek**: 使用提供的代理服务

4. **切换模型**
   - 点击任意已配置的模型卡片
   - 切换成功后立即生效

### 新增文件

**工具层**:
- `utils/aiService.js` - AI 服务统一入口
- `utils/aiAdapters.js` - AI 适配器（DeepSeek/Gemini/Custom）
- `utils/encryptStorage.js` - 加密存储工具

**逻辑层**:
- `pages/index/composables/useAIConfig.js` - AI 配置管理

**UI 层**:
- `pages/config/ai-selector.vue` - AI 模型选择页面

**文档**:
- `AI_GUIDE.md` - AI 系统使用指南

---

## 💬 2025年12月4日 - 聊天对话窗口功能

### 核心特性
- 💬 App 内独立聊天 Tab
- 🤖 AI 对话（携带宠物状态上下文）
- 💾 消息历史持久化（最近100条）
- ⚡ 4 个快捷回复按钮
- 🎨 赛博朋克风格 UI

### 聊天功能

#### 消息类型
1. **用户消息**: 橙色气泡，右对齐
2. **宠物消息**: 青色气泡，左对齐，带情绪颜色
3. **系统消息**: 灰色气泡，居中显示

#### 快捷回复
预设了 4 个常用问题：
- 📚 "今天学了多久？"
- 📊 "查看我的数据"
- ❓ "你是谁？"
- 💪 "鼓励我一下"

#### AI 上下文集成
宠物 AI 会根据以下信息回复：
- 等级 (Level)
- 心情值 (Mood)
- 今日学习时长
- 今日摸鱼时长

### 新增文件
- `pages/index/composables/useChat.js` - 聊天系统逻辑
- `pages/index/components/ChatPanel.vue` - 聊天面板
- `pages/index/components/ChatBubble.vue` - 消息气泡组件

### 修改文件
- `pages/index/index.vue` - 集成聊天系统
- `pages/index/components/TabSwitch.vue` - 新增对话 Tab
- `pages/index/composables/useAI.js` - 扩展 AI 功能

---

## 📊 宠物成长系统（代码层面 - 未集成）

### ⚠️ 重要提示
虽然成长系统的代码已创建，但 **尚未集成到主界面和监控系统**，用户暂时无法看到数值变化效果。

### 已创建的功能代码

#### 1. 核心数值模型
- 等级 (Level): 1-99
- 经验值 (XP): 通过学习获得
- 心情值 (Mood): 0-100
- 金币 (Coins): 学习奖励
- 饥饿度 (Hunger): 0-100
- 亲密度 (Bond): 0-100

#### 2. 宠物形态系统
**4 种宠物类型**:
- 👻 幽灵 (默认)
- 🐕 中华田园犬
- 🦜 玄凤鹦鹉
- 🦜 和尚鹦鹉

**6 种形态状态**:
- 🥚 蛋形态 (Lv1-5)
- 🐣 幼年形态 (Lv6-15)
- ✨ 成年形态 (Lv16+)
- 😈 邪恶形态 (心情 < 20)
- 😇 天使形态 (心情 > 80)
- 🤖 机械形态 (亲密度 100)

#### 3. 奖惩机制
**学习奖励**:
```
5分钟:  +10 XP, +5 Coins, +5 Mood
15分钟: +30 XP, +15 Coins, +10 Mood
30分钟: +60 XP, +30 Coins, +15 Mood
60分钟: +150 XP, +100 Coins, +20 Mood
```

**摸鱼惩罚**:
```
10分钟:  -10 Mood
30分钟:  -20 Mood, -10 Hunger
60分钟:  -30 Mood, -20 Coins, -20 Hunger
180分钟: -50 Mood, -50 Coins, -50 Hunger
```

**自然衰减**:
- 每小时饥饿度 -5
- 心情值回归中位值（50）

### 已创建文件
- `pages/index/composables/useGrowth.js` - 成长系统核心逻辑
- `pages/index/utils/growthFormula.js` - 数值计算公式
- `pages/index/utils/petForms.js` - 宠物形态配置

### 下一步计划
要让成长系统真正生效，需要：
1. 在 `index.vue` 中集成 `useGrowth` composable
2. 监控系统检测到学习/摸鱼时调用奖惩逻辑
3. 在 `PetScreen.vue` 中显示数值进度条
4. 在 `StatusBar.vue` 中显示等级和金币

---

## 📝 使用指南

### 如何体验聊天功能
1. 打开 App，点击底部 **"💬 对话"** Tab
2. 可以：
   - 点击快捷回复按钮
   - 手动输入消息并发送
3. 宠物会根据当前状态给出不同回复
4. 点击"加载更早的消息"查看历史记录

### 如何切换 AI 模型
1. 打开 App → **"系统设置"** → **"🤖 AI 模型选择"**
2. 配置想要使用的模型的 API Key
3. 点击"测试连接"验证配置
4. 点击模型卡片切换

### 如何查看日志
查看 HBuilderX 控制台输出，关键日志前缀：
- `[AI Service]` - AI 服务相关
- `[useChat]` - 聊天系统相关
- `[ChatPanel]` - 聊天面板相关
- `[index.vue]` - 主界面相关

---

## 🔧 技术细节

### ref 响应性修复原理
```javascript
// ❌ 错误做法 - ref 会被自动解包
this.chat = useChat();
console.log(this.chat.userInput); // 'hello' (字符串，失去响应性)

// ✅ 正确做法 - 保持 ref 引用
const chatComposable = useChat();
this.chatMessages = chatComposable.messages;
this.chatUserInput = chatComposable.userInput; // Ref<string>
console.log(this.chatUserInput.value); // 'hello' (可响应)
```

### 消息存储格式
```javascript
{
  id: 'msg_1733364000000_abc123',
  role: 'user' | 'pet' | 'system',
  content: '消息内容',
  timestamp: 1733364000000,
  type: 'text',
  emotion: 'normal' | 'happy' | 'angry',
  metadata: { /* 额外数据 */ }
}
```

### API 适配器架构
```javascript
// 统一接口
interface AIAdapter {
  chat(userMessage: string, systemPrompt: string, history: Array): Promise<string>
}

// 工厂模式
const ADAPTER_MAP = {
  'deepseek': DeepSeekAdapter,
  'gemini': GeminiAdapter,
  'custom': CustomAdapter
};
```

---

## 🐛 已知问题

### 已解决
- ✅ 聊天输入框无法输入
- ✅ 历史消息顺序混乱
- ✅ 消息重复显示

### 待解决
- ⏳ 成长系统未集成到 UI
- ⏳ 悬浮窗内无法聊天
- ⏳ 没有升级动画效果

---

## 📚 相关文档

- **[AI_GUIDE.md](./AI_GUIDE.md)** - AI 多模型系统详细指南
- **[README.md](./README.md)** - 项目总体介绍
- **[REFACTORING.md](./REFACTORING.md)** - 代码重构记录

---

**最后更新**: 2025年12月5日
