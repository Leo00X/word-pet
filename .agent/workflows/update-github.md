---
description: 
---

请帮我提交当前的代码变动到 GitHub。

1. 执行 `git status` 检查变动文件。
2. 将所有变动添加到暂存区 (`git add .`)。
3. 根据刚才我们完成的工作（如：聊天对话窗口、宠物成长系统、UI组件化重构等），自动生成一个符合 Conventional Commits 规范的中文 Commit 信息。
4. 执行提交并推送到远程仓库 (`git push`)。